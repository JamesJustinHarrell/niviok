//collection of Desal values

using System.Collections.Generic;

static class xxx_Objects {
	/* xxx
	class FunctionSignature {
		public IList<Parameter> parameters;
		public IInterface returnInterface;
	}
	
	static IDictionary<FunctionSignature, IInterface> functionInterfaces;

	public static IInterface FuncInterface = new Interface(
		new IInterface[]{},
		new PropertyInfo[]{},
		new MethodInfo[]{} );

	public static IInterface Interface = new Interface(
		new IInterface[]{},
		new PropertyInfo[]{},
		new MethodInfo[]{} );

	public static IInterface Object = new Interface(
		new IInterface[]{},
		new PropertyInfo[]{},
		new MethodInfo[]{} );

	public static IInterface String = new Interface(
		new IInterface[]{},
		new PropertyInfo[]{},
		new MethodInfo[]{} );
	
	
	public static IInterface getFunctionInterface(IFunction function) {
		return FunctionInterface.getFuncFace( function.parameters, function.returnType );
	}*/
}